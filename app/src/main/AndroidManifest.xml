<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.polant.projectsport" >

    <application
        tools:replace="android:icon"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:screenOrientation="portrait"
            android:name=".MainActivity" >
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".activity.ArticleInfoActivity"
            />
        <activity
            android:screenOrientation="portrait"
            android:name=".activity.ActivityCalculateFood"
            />
        <activity
            android:name=".preferences.PreferencesNewActivity"
            android:label="@string/preferences_title" >
        </activity>
        <activity
            android:name=".preferences.PreferencesOldActivity"
            android:label="@string/preferences_title" >
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".activity.ActivityOtherCalculators"
            android:label="@string/title_activity_activity_other_calculators"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
